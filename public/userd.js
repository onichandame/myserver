$(document).ready(function(){
  /*
  const fileappd='apps.json'
  let xhr=new XMLHttpRequest()
  xhr.open('GET','/'+fileappd)
  xhr.send()
  xhr.onload=function(){
    if(xhr.status==200)
      updateappd(xhr.responseText)
  }
  $('.dropdown').hover(()=>{
    $(this).addClass('show')
    $(this).find('[data-toggle="dropdown"').attr('aria-expanded',true)
    $(this).find('.dropdown-menu').addClass('show')
  },()=>{
    $(this).removeClass('show')
    $(this).find('[data-toggle="dropdown"').attr('aria-expanded',false)
    $(this).find('.dropdown-menu').removeClass('show')
  })
  function updateappd(text){
    try{
      const appd=JSON.parse(text)
      appd.forEach((app)=>{
        $('.app .dropdown-menu').append(addappd(app))
      })
      function addappd(app){
        return '<a class="dropdown-item" href="'+app.link+'">'+app.title+'</a>'
      }
    }catch(e){
    }
  }
  */
})
